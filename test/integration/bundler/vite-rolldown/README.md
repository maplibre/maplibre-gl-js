# Vite 8+ (Rolldown) example

Minimal Vite 8+ (Rolldown) app exercising the ESM build and worker auto-detection.

## Setup

From the repo root, build the parent package once so `dist/` is populated:

```bash
npm install
npm run build-dist
```

Then in this directory:

```bash
npm install
npm run dev
```
