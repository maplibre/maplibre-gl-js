name: Upload Benchmarks

on:
  push:
    branches:
      - main
  workflow_call:
    inputs:
      tag:
        description: 'Tagged commit to upload (e.g., v1.2.3). Leave empty for main branch.'
        required: false
        type: string
  workflow_dispatch:
    inputs:
      tag:
        description: 'Tagged commit to upload (e.g., v1.2.3). Leave empty for main branch.'
        required: false
        type: string

jobs:
  upload_benchmarks:
    runs-on: ubuntu-latest
    steps:
      - name: Determine ref
        if: inputs.tag != ''
        id: ref
        run: |
          TAG="${{ inputs.tag }}"
          echo "ref=refs/tags/${TAG}" >> $GITHUB_OUTPUT

      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          ref: ${{ steps.ref.outputs.ref || 'refs/heads/main' }}

      - name: Use Node.js from nvmrc
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v4
        with:
          node-version-file: '.nvmrc'

      - name: Install
        run: npm ci

      - name: Build benchmarks
        run: |
          npm run generate-typings
          npm run build-benchmarks

      - name: Copy ESM benchmark artifacts
        run: |
          mkdir tmp
          cp test/bench/versions/benchmarks_generated.mjs tmp
          cp test/bench/versions/benchmarks_generated.mjs.map tmp
          cp test/bench/versions/benchmarks_worker.mjs tmp
          cp test/bench/versions/benchmarks_worker.mjs.map tmp

      - name: Upload to GitHub Pages
        uses: peaceiris/actions-gh-pages@84c30a85c19949d7eee79c4ff27748b70285e453 # v4.1.0
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: tmp
          destination_dir: benchmarks/${{ inputs.tag || 'main' }}
          commit_message: 'deploy: ${{ inputs.tag }}'

