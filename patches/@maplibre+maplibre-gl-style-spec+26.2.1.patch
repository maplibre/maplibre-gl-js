diff --git a/node_modules/@maplibre/maplibre-gl-style-spec/dist/index.cjs b/node_modules/@maplibre/maplibre-gl-style-spec/dist/index.cjs
index 4fd5a08..f4d6586 100644
--- a/node_modules/@maplibre/maplibre-gl-style-spec/dist/index.cjs
+++ b/node_modules/@maplibre/maplibre-gl-style-spec/dist/index.cjs
@@ -545,6 +545,8 @@
 			}
 		},
 		layout_symbol: {
+"symbol-height-offset": { "type": "number", "default": 0, "units": "meters", "doc": "Offsets the icon and text vertically by the given number of meters. The datum the offset is measured from is controlled by `symbol-height-anchor`. Negative values lower the symbol.", "requires": [ { "symbol-placement": [ "point" ] } ], "expression": { "interpolated": true, "parameters": [ "zoom", "feature" ] }, "property-type": "data-driven" },
+"symbol-height-anchor": { "type": "enum", "values": { "ground": { "doc": "`symbol-height-offset` is measured upwards from the terrain surface below the symbol, so the symbol follows the terrain." }, "absolute": { "doc": "`symbol-height-offset` is measured upwards from the zero elevation datum, so the symbol ignores the terrain below it." } }, "default": "ground", "doc": "Selects the datum that `symbol-height-offset` is measured from.", "requires": [ "symbol-height-offset", { "symbol-placement": [ "point" ] } ], "expression": { "interpolated": false, "parameters": [ "zoom" ] }, "property-type": "data-constant" },
 			"symbol-placement": {
 				"type": "enum",
 				"values": {
diff --git a/node_modules/@maplibre/maplibre-gl-style-spec/dist/index.mjs b/node_modules/@maplibre/maplibre-gl-style-spec/dist/index.mjs
index 1f8cb92..6a5d4ca 100644
--- a/node_modules/@maplibre/maplibre-gl-style-spec/dist/index.mjs
+++ b/node_modules/@maplibre/maplibre-gl-style-spec/dist/index.mjs
@@ -541,6 +541,8 @@ var v8_default = {
 		}
 	},
 	layout_symbol: {
+"symbol-height-offset": { "type": "number", "default": 0, "units": "meters", "doc": "Offsets the icon and text vertically by the given number of meters. The datum the offset is measured from is controlled by `symbol-height-anchor`. Negative values lower the symbol.", "requires": [ { "symbol-placement": [ "point" ] } ], "expression": { "interpolated": true, "parameters": [ "zoom", "feature" ] }, "property-type": "data-driven" },
+"symbol-height-anchor": { "type": "enum", "values": { "ground": { "doc": "`symbol-height-offset` is measured upwards from the terrain surface below the symbol, so the symbol follows the terrain." }, "absolute": { "doc": "`symbol-height-offset` is measured upwards from the zero elevation datum, so the symbol ignores the terrain below it." } }, "default": "ground", "doc": "Selects the datum that `symbol-height-offset` is measured from.", "requires": [ "symbol-height-offset", { "symbol-placement": [ "point" ] } ], "expression": { "interpolated": false, "parameters": [ "zoom" ] }, "property-type": "data-constant" },
 		"symbol-placement": {
 			"type": "enum",
 			"values": {
diff --git a/node_modules/@maplibre/maplibre-gl-style-spec/dist/latest.json b/node_modules/@maplibre/maplibre-gl-style-spec/dist/latest.json
index 95de93c..945f2fc 100644
--- a/node_modules/@maplibre/maplibre-gl-style-spec/dist/latest.json
+++ b/node_modules/@maplibre/maplibre-gl-style-spec/dist/latest.json
@@ -1388,6 +1388,8 @@
     }
   },
   "layout_symbol": {
+"symbol-height-offset": { "type": "number", "default": 0, "units": "meters", "doc": "Offsets the icon and text vertically by the given number of meters. The datum the offset is measured from is controlled by `symbol-height-anchor`. Negative values lower the symbol.", "requires": [ { "symbol-placement": [ "point" ] } ], "expression": { "interpolated": true, "parameters": [ "zoom", "feature" ] }, "property-type": "data-driven" },
+"symbol-height-anchor": { "type": "enum", "values": { "ground": { "doc": "`symbol-height-offset` is measured upwards from the terrain surface below the symbol, so the symbol follows the terrain." }, "absolute": { "doc": "`symbol-height-offset` is measured upwards from the zero elevation datum, so the symbol ignores the terrain below it." } }, "default": "ground", "doc": "Selects the datum that `symbol-height-offset` is measured from.", "requires": [ "symbol-height-offset", { "symbol-placement": [ "point" ] } ], "expression": { "interpolated": false, "parameters": [ "zoom" ] }, "property-type": "data-constant" },
     "symbol-placement": {
       "type": "enum",
       "values": {
