{
    "name": "MapLibre container using node.js",
    "postCreateCommand": "sudo apt-get update && sudo apt-get install build-essential git libglew-dev libxi-dev default-jre default-jdk xvfb && nvm install && npm install"
}