/**
 * WebGPU Renderer for MapLibre GL JS — skeleton for future use.
 * TODO: Extract WebGPU-specific code from drawable.ts and painter.ts into this class.
 */
export class WebGPURenderer {
    readonly type = 'webgpu' as const;
}
